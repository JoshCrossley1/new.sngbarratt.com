import { SngTranslateDirective } from './sng-translate.directive';

describe('SngTranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new SngTranslateDirective();
    expect(directive).toBeTruthy();
  });
});
