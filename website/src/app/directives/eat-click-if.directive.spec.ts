import { EatClickIfDirective } from './eat-click-if.directive';

describe('EatClickIfDirective', () => {
  it('should create an instance', () => {
    const directive = new EatClickIfDirective();
    expect(directive).toBeTruthy();
  });
});
